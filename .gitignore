# 依赖
node_modules/

# 构建产物
dist/
build/

# 环境变量（含密钥，切勿提交）
.env
.env.local
.env.*.local

# 日志
*.log
npm-debug.log*
*.out.log
*.err.log

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 缓存
.cache/
*.tsbuildinfo
__pycache__/
*.pyc

# 本地运行数据：音色库、任务、输出音频、训练实验
# 这些是「本机产物」，换一台机器就该重来，不应进入版本库
trainer/.data/
trainer/.tmp/

# GPT-SoVITS 官方整合包：自带 runtime/（Python + torch）与预训练权重，14 GB 级别。
# 只在本机就地保留，由启动脚本自动探测，绝不进入版本库。
GPT-SoVITS*/

# 线上静态版（mimo-voice-studio）的部署产物，内含一份独立的部署仓库
.pages/

# 本地编辑器 / AI 工具配置
.codebuddy/
